defaults
{
    log_type = SYSLOG daemon info
    log_on_success = HOST PID
    log_on_failure = HOST
}

service telnet
{
    disable = no
    socket_type = stream
    protocol = tcp
    wait = no
    user = root
    server = /usr/sbin/telnetd
}
