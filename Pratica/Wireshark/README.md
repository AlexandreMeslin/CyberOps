# Laboratório de Wireshark

1. Inicie a captura na interface correta
1. Gere uma única comunicação
1. Pare a captura
1. Use o protocolo correto para filtrar a captura (por exemplo, `http`)
1. Selecione datagrama e use `Follow` > `TCP Strem` (oiu `HTTP Stream`)
1. Relacione o conteúdo com os segmentos TCP